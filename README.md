# Smart API Gateway

A performant API Gateway written in Go with routing, rate limiting, JWT authentication, and more.

## Features
- Reverse proxy routing
- JWT authentication
- Rate limiting with Redis
- Configurable via YAML

## Getting Started
1. Clone the repo
2. Run `go run ./cmd/main.go`
3. Configure your routes in `configs/routes.yaml`
