routes:
  - path: /api/service-a
    target: http://localhost:8001
    refillRate: 1
    capacity: 10
    authRequired: false
  - path: /api/service-b
    target: http://localhost:8002
    refillRate: 1
    capacity: 10
    authRequired: false

server:
  host: localhost
  port: 8000

logging:
  level: info
